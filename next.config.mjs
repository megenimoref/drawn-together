/** @type {import('next').NextConfig} */
const nextConfig = {
  /* פלט standalone — image קטן ל-Docker (server.js עצמאי + node_modules מינימלי). */
  output: 'standalone'
};

export default nextConfig;
