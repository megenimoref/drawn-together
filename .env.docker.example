# העתיקו ל-.env (docker-compose קורא אותו אוטומטית) לפני `docker compose up`.

# כתובת הפרודקשן הציבורית (הדומיין שמפנה ל-nginx). חובה לקישורי שיתוף/QR נכונים.
NEXT_PUBLIC_SITE_URL=https://drawn-together.example.com

# אופציונלי — שחזור קישור פרטי במייל (Resend)
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxx
# RECOVERY_EMAIL_FROM=onboarding@resend.dev
